# PyCharm 自动补全 QueueManager 接入 (2026-04-07 ~ 04-10)

## 背景

VS Code 侧通过 `ContinueCompletionProvider` 创建独立的 `QueueManager`，事件驱动预收集 snippet。
PyCharm/IntelliJ 侧通过 `Core → CompletionProvider`，但 Core 创建 CompletionProvider 时**未传入 QueueManager**，
导致始终走 legacy path（on-demand 收集），totalContextMs 390-472ms。

## 根因分析

| #   | 根因                                                                            | 影响                  |
| --- | ------------------------------------------------------------------------------- | --------------------- |
| 1   | `core.ts` 创建 CompletionProvider 时只传 5 参数，缺少第 6 参数 QueueManager     | 永远走 legacy path    |
| 2   | `shouldPrefilter()` 重复调用 `getWorkspaceDirs()` + `readFile(.continueignore)` | prefilterMs 107-140ms |
| 3   | `HelperVars.create()` 外部计时 vs 内部计时有 ~150ms 差距                        | 需诊断                |

## 实施方案

### Phase 1: Core.ts QueueManager 接入

**文件**: `core/core.ts`

1. Import `QueueManager` from `./autocomplete/context/QueueManager`
2. 在 Core 类中新增 `private queueManager: QueueManager` 属性
3. 构造时 `this.queueManager = new QueueManager()` 并传入 CompletionProvider 第 6 参数

### Phase 1.2: 事件接线

**文件**: `core/core.ts`

在 `files/opened` handler 中：

- 读取文件前 30 行 header 内容
- `queueManager.pushOpenedFile()` 推入 openedFilesQueue
- `queueManager.markCoreReady()` 标记同步队列可用
- `queueManager.startWarming()` + `completionProvider.warmFile()` 异步预热 import

在 `files/closed` handler 中：

- `queueManager.removeOpenedFile()` 清理已关闭文件

### Phase 1.3: 回填逻辑

**文件**: `core/autocomplete/CompletionProvider.ts`

当 legacy path 执行后，将收集到的 openedFileSnippets 和 importDefinitionSnippets 回填到 QueueManager，
标记 core-ready + full-ready，使后续请求走 fast path。

### Phase 2: Prefilter IPC 缓存

**文件**: `core/autocomplete/prefiltering/index.ts`

1. 模块级缓存 `_cachedWorkspaceIgArray`（TTL 30s）避免每次请求读取 `.continueignore`
2. `isDisabledForFile()` 改为同步，直接接收 `workspaceDirs` 参数（来自 `helper.workspaceUris`），不再重复 IPC
3. 预期节省 prefilterMs ~100ms

### Phase 3: HelperVars 诊断计时

**文件**: `core/autocomplete/util/HelperVars.ts`

在 `create()` 中添加 `Date.now()` 打点，输出 `create() total=Xms`，
与已有的 `[HelperVars SubTimings]` 配合，定位外部-内部计时差距来源。

## 安全分析

- VS Code 不通过 Core 发送 `autocomplete/complete` → Core 的 CompletionProvider + QueueManager 对 VS Code 侧无影响
- QueueManager 是实例级对象，不干扰 VS Code 侧独立的 QueueManager 实例
- 未就绪时 fallback 到 legacy path → 零退化风险
- IntelliJ 已发送 `files/opened`/`files/closed`/`files/changed` 事件 → 无需 Kotlin 改动

## 变更文件清单

| 文件                                               | 变更类型                          |
| -------------------------------------------------- | --------------------------------- |
| `core/core.ts`                                     | 新增 QueueManager 实例 + 事件接线 |
| `core/autocomplete/CompletionProvider.ts`          | legacy path 回填逻辑              |
| `core/autocomplete/prefiltering/index.ts`          | IPC 缓存 + 参数优化               |
| `core/autocomplete/util/HelperVars.ts`             | 诊断计时                          |
| `extensions/intellij/.../DocumentChangeTracker.kt` | 发送编辑事件到 Core               |
| `extensions/intellij/.../ActiveHandlerManager.kt`  | 发送光标移动事件到 Core           |

## 统一事件处理实现 (Phase 1.4)

### 架构说明

VSCode 和 IntelliJ 采用不同的架构：

- **VSCode**: 单进程架构，拥有独立的 `ContinueCompletionProvider` 和 `QueueManager`
- **IntelliJ**: 多进程架构，IDE 进程 + Core 进程，通过 IPC 通信

为了使 IntelliJ 也能利用 QueueManager 的预收集能力，在 Core.ts 中添加了统一的事件处理：

### Core.ts 事件处理

```typescript
// files/smallEdit - 编辑事件
on("files/smallEdit", async ({ data }) => {
  if (data.actions && data.actions.length > 0) {
    for (const action of data.actions) {
      if (action.filepath && action.editText) {
        this.queueManager.pushEdited(
          action.filepath,
          content,
          paddedStart,
          paddedEnd,
        );
      }
    }
  }
});

// files/cursorMoved - 光标移动事件
on("files/cursorMoved", async ({ data }) => {
  this.queueManager.pushVisited(
    data.filepath,
    data.content,
    data.startLine,
    data.endLine,
  );
});
```

### IntelliJ 端事件发送

**DocumentChangeTracker.kt** - 发送编辑事件：

```kotlin
private fun sendEditEventToCore(event: DocumentEvent) {
    val editAction = mapOf(
        "filepath" to filepath,
        "range" to mapOf(...),
        "fileContents" to fileContents,
        "editText" to event.newFragment.toString(),
        ...
    )
    coreMessenger?.request("files/smallEdit", mapOf(
        "actions" to listOf(editAction)
    ), null) { }
}
```

**ActiveHandlerManager.kt** - 发送光标移动事件：

```kotlin
private fun sendCursorMovedEventToCore(editor: Editor, position: LogicalPosition) {
    // 提取光标周围 ±30 行内容
    val content = document.getText(TextRange(startOffset, endOffset)).trim()
    coreMessenger?.request("files/cursorMoved", mapOf(
        "filepath" to filepath,
        "content" to content,
        "startLine" to startLine,
        "endLine" to endLine
    ), null) { }
}
```

### 性能优化点

1. **零 IO**: 内容直接从内存中的 document 对象提取，不涉及文件系统读取
2. **非阻塞**: 事件发送是异步的，不阻塞 UI 线程
3. **预收集**: 编辑和光标移动时预收集上下文，autocomplete 时直接使用队列中的数据

---

# 自动补全质量诊断：7 个核心问题根因分析 (2026-04-08)

## 日志来源

- **VS Code**: `jn-vscode-20260408215000.txt` — VS Code Insiders + continue-1.3.39
- **PyCharm**: `jn-pycharm-20260408215300.log` — PyCharm CE 2024.3 + binary core
- **推理后端（本次日志样本）**: sglang `http://autocomplete.sglang.ai.infra/v1/completions`
- **模型**: `Qwen3-Coder-30B-A3B-Instruct` / `Qwen3-Coder-Next-AWQ-8bit`

当前修复方案要求 **同时兼容 vLLM 和 sglang**。因此文中的后端优化若不是在描述本次日志事实，都应理解为“推理后端能力抽象”，而不是把控制逻辑写死到 sglang。

## 日志统计摘要

### PyCharm (38秒内, 13:17:30 → 13:18:08)

| 指标                            | 数值    |
| ------------------------------- | ------- |
| 总请求数 (StreamRaw)            | 11      |
| 成功返回 (Result)               | 4       |
| 被 abort / 空结果 (EmptyResult) | 7       |
| **成功率**                      | **36%** |
| 平均成功补全长度                | 30 字符 |
| abort 次数                      | 5       |
| rawLen=0 的请求                 | 7       |

### VS Code (同一时段)

| 指标                      | 数值                      |
| ------------------------- | ------------------------- |
| abort 错误 (llm_fetch)    | ~10+                      |
| rawLen=0 或 1             | 大量                      |
| PostReject reason=isBlank | 5+                        |
| 成功返回有内容 (rawLen>1) | 2 (rawLen=138, rawLen=12) |

### 关键时间序列异常

**异常 1: 13:17:50 PyCharm abort 级联** — 3 个请求在 1 秒内连续触发并互相取消：

```
13:17:50 ba6a97f1 → abort → rawLen=0 → PostReject isBlank
13:17:50 11f61bc1 → abort → rawLen=0 → PostReject isBlank
13:17:52 ac9f5028 →         rawLen=0 → PostReject isBlank (被下一个请求占用推理后端队列；样本环境为 sglang)
```

**异常 2: VS Code 双重 error 日志** — 每次 abort 产生两条 error（`llm_fetch` + `llm_stream_complete`）：

```
ERR: The operation was aborted. {"context":"llm_fetch","url":"http://autocomplete.sglang.ai.infra/v1/completions",...}
ERR: The operation was aborted. {"context":"llm_stream_complete","model":"Qwen3-Coder-30B-A3B-Instruct",...}
```

根因：`llm/index.ts` 中同一个 AbortError 在调用链冒泡时被 **两层 catch** 各捕获一次——L507 `fetch()` 方法（`context: "llm_fetch"`）和 L813 `streamComplete()` 方法（`context: "llm_stream_complete"`），而非之前推测的"第二个 AbortController"。

**异常 3: `mock:///` URI 出现在 PyCharm prompt** — `<|file_sep|>mock:///bpp_case_tools.py`，JetBrains 虚拟文件 scheme 未被正规化。

---

## 问题 1: 服务器端 abort 数量过多，影响性能

### 根因链

```
用户连续击键
  → debounce 定时器重置
  → 但前一个请求已通过 debounce，正在推理后端流式推理（本次样本环境为 sglang）
  → GeneratorReuseManager.getGenerator() 发现 prefix 不匹配
  → ListenableGenerator.cancel() 触发 AbortController.abort()
  → fetch 侧中止请求 / 关闭客户端流
  → 已开始的 prefill / decode 白做 → KV cache 占用直到后端完成清理
```

### 代码路径

| 步骤                      | 文件                       | 位置 | 说明                                                                 |
| ------------------------- | -------------------------- | ---- | -------------------------------------------------------------------- |
| 1. 创建主 AbortController | `CompletionProvider.ts`    | L237 | 存入 `_abortControllers` Map                                         |
| 2. 创建副 AbortController | `GeneratorReuseManager.ts` | L49  | 包装到 ListenableGenerator                                           |
| 3. prefix 不匹配触发取消  | `GeneratorReuseManager.ts` | L49  | `_createListenableGenerator()` 中取消旧 generator                    |
| 4. abort 传播到 fetch     | `llm/index.ts`             | L507 | `fetch()` 的 catch 记录第一条 error (`llm_fetch`)                    |
| 5. 错误冒泡再次被捕获     | `llm/index.ts`             | L813 | `streamComplete()` 的 catch 记录第二条 error (`llm_stream_complete`) |

### 影响量化

每次 abort：

- 浪费推理后端 GPU 推理（prefill + 已生成 decode 步）
- 占用推理后端 KV cache slot（8K token prompt ≈ 几十 MB 显存）直至清理
- 产生 **2 条 error 日志**（`llm_fetch` + `llm_stream_complete`）——同一个 AbortError 在 `llm/index.ts` 的两层 catch 中各被捕获一次
- 网络往返开销（TCP 建连 + RST）

### 更深一层的根因（架构级）

如果只把问题归因成「debounce 太小」或「取消不够快」，还是停留在现象层。更根上的问题是：

**当前 autocomplete 控制链没有一个显式的“请求准入 / 生命周期协调层”。**

现在的职责分散在多个点：

- `CompletionProvider.ts` 负责 editor event 入口、debounce、上下文收集和发起生成
- `GeneratorReuseManager.ts` 负责 generator 复用与取消旧 generator
- `CompletionStreamer.ts` 负责流式过滤与 full stop
- `llm/index.ts` 负责 fetch / streamComplete 的底层取消

但这几层之间没有一个统一状态机去回答下面这些关键问题：

1. 当前请求是否只是“候选请求”，还是已经被允许占用模型资源？
2. 同一文档 / 同一模型当前是否已经有 in-flight 生成？
3. 新请求到来时，应当直接替换旧请求、等待旧请求、复用旧请求，还是只更新最新快照？
4. 旧请求是在首 token 前被取消，还是已经进入有效流式阶段后才被取消？

结果就是：**abort 被当成主要控制手段，而不是调度层的例外收尾动作。**

这才是 abort 率偏高的架构级根因。动态 debounce 下限过低，只是在这个架构缺口上继续放大了重叠请求。

### 修复方案

**方案 A (P0): 降低 abort 日志级别**

文件: `core/llm/index.ts` L507 (`fetch()` 方法) 和 L813 (`streamComplete()` 方法)

```typescript
// 当前: Logger.error(e, { context: "llm_fetch", ... });
// 改为:
if (isAbortError(e)) {
  Logger.debug("Autocomplete request aborted", { context: "llm_fetch", ... });
} else {
  Logger.error(e, { context: "llm_fetch", ... });
}
```

同理修改 `streamComplete()` 的 catch (L813)。双日志的根因是同一个 AbortError 在调用链冒泡时被两层 catch 各捕获一次，两处都需要降级。

**方案 B (P1): 引入 latest-only 请求准入 / 生命周期协调层**

建议在 `CompletionProvider` 与 `CompletionStreamer` 之间引入一个显式协调层，统一管理请求状态，而不是把控制逻辑散落在 debounce、generator reuse 和 fetch catch 中。

这个协调层至少要管理 5 个状态：

1. `captured`：收到 editor event，但尚未准入
2. `debounced`：通过输入合并，成为最新候选
3. `admitted`：允许占用一次真实生成资源
4. `streaming`：已进入首 token 后的流式阶段
5. `superseded/cancelled`：被新请求取代或显式取消

核心策略应是：

- **latest-only admission**：同一文档 / 模型同一时刻只允许一个真实 in-flight 生成占用资源
- **first-token-aware cancellation**：区分首 token 前取消与首 token 后取消
- **reuse-aware handoff**：只有满足 prefix 复用条件时才把控制权交给 `GeneratorReuseManager`
- **debounce 只是输入侧信号，不再承担主要调度责任**

这样才能把 abort 从“主要控制手段”降回“调度后的收尾动作”。

**方案 C (P2): adaptive debounce 作为协调层策略，而不是单独补丁**

`AutocompleteDebouncer.ts` 的 30ms pause bucket 问题是真实存在的，但不应再作为单独的“根因修复项”存在。更合理的做法是：先建立请求准入协调层，再把 adaptive debounce 作为协调层的一条输入策略，根据 in-flight 状态、最近 `streamMs` / TTFT、是否已有首 token 等信息动态调整。

**方案 D (P2): 推理后端取消能力抽象（兼容 vLLM / sglang）**

abort 时不要把服务端资源释放逻辑写死成 sglang `/abort`。更合理的做法是在 `llm` provider 层增加统一能力抽象：

1. core 调度层只表达“取消这个 in-flight 请求”，不直接感知具体后端
2. sglang 如果具备显式 server-side abort 能力，则在 provider 适配层调用对应接口
3. vLLM 如果当前只有客户端断流能力，就优雅回退到 `AbortSignal` / 连接关闭；如果后续有稳定的 request-level cancel 能力，也在 provider 层接入

这样请求协调层保持后端无关，后端差异只落在 provider 适配层。

---

## 问题 2: 自动补全多数情况出不来，PyCharm 最常见

### PyCharm 特有根因 (相比 VS Code 额外的 4 个因素)

#### 2a. JetBrains 缺少 abort 信号传播 (最关键)

**文件**: `extensions/intellij/.../ContinueCompletionService.kt`

```kotlin
// 当前: 不传 AbortSignal，无法从 IDE 端取消 core 请求
coreMessenger?.request("autocomplete/complete", requestInput, null) { }
```

当 JetBrains 端 `withTimeoutOrNull(modelTimeout * 3)` 超时返回 null 后，core 端的 LLM 请求 **仍在运行**。下次击键触发新请求时，新旧请求会在推理后端并发竞争；本次日志样本里表现为 sglang 队列拥堵，但这个问题并不局限于 sglang。

**VS Code 对比**: `AbortController` 从 `CompletionProvider` 一路传到 `fetch(url, { signal })`，超时后清理干净。

#### 2b. 固定间隔 debouncer

**文件**: `extensions/intellij/.../Debouncer.kt`

```kotlin
// JetBrains: 简单固定延迟
fun debounce(action: suspend () -> Unit) {
    debounceJob?.cancel()
    debounceJob = coroutineScope.launch { delay(interval); action() }
}
```

**VS Code**: `AutocompleteDebouncer.ts` 有自适应延迟：

- 快速打字 (<80ms) → 150-200ms 延迟（合并请求）
- 正常打字 → 80ms
- 停顿 (>200ms) → 30ms（快速响应）

JetBrains 没有这种自适应能力，快速打字时发出更多无效请求。

但进一步看 VS Code 侧，这个自适应实现也不是没有问题：在 `AutocompleteDebouncer.ts` 中，停顿分支会**直接返回 30ms**，绕过 `debounceDelay` 的基准值；而在本次 VS Code 日志中，`debounceWaitMs` 大多数都落在 29-32ms，说明当前策略几乎长期停在这个最激进的快速路径。

结合日志中的另外两组事实：

- `totalContextMs` 在多数请求里仅 2-4ms，说明 debounce 后请求会非常快地推进到 LLM
- `streamMs` 常落在 500-3100ms，说明前一个请求往往仍在流式解码阶段

这意味着：**30ms 下限不是根因本身，而是会显著放大现有架构缺口上的重叠请求和 abort 率。**

更准确的定性应该是：

- JetBrains 的问题是“固定 debounce + 缺取消传播”
- VS Code 的问题是“已有 adaptive debounce，但 pause bucket 下限过低，且完全不感知在飞请求/最近流式延迟”

因此，这里更合理的结论不是“继续给 debouncer 打补丁”，而是：**先补齐统一请求协调层，再升级 VS Code / JetBrains 两端的 debounce 策略。**

#### 2c. `mock:///` URI 导致上下文收集和路径展示异常

JetBrains 对虚拟文件返回 `mock:///` scheme，这是一个 **URI 规范化缺失** 的系统性问题，影响多个层面：

- `AutocompleteTemplate.ts` 中 `getFileName()` 只检查 `startsWith("file://")` → `mock:///` 原样写入 LLM prompt（如 `<|file_sep|>mock:///bpp_case_tools.py`）
- `core/util/uri.ts` 中 `findUriInDirs()` 做 scheme 匹配 → `mock:///` 与 `file://` workspace 不匹配 → 文件被跳过
- `core/util/filesystem.ts` 中 `fileURLToPath()` 对 `mock:///` 会抛异常（但不在 ImportDefinitionsService 直接调用链上）

修复落点不是某个具体 Service，而应在：

- **首选**: JetBrains 扩展端上报 URI 前规范化为 `file://`
- **兜底**: core template 层对非 `file://` URI 做降级处理（如提取路径部分）

#### 2d. 文档仅 ±5 行增量同步

**文件**: `extensions/intellij/.../DocumentChangeTracker.kt`

```kotlin
val paddedStart = Math.max(0, startLine - 5)
val paddedEnd = Math.min(document.lineCount - 1, endLine + 5)
```

core 可能持有过期文件内容。

### 通用根因 (VS Code 也存在)

#### 2e. `noDoubleNewLine()` 过滤器杀死补全

**文件**: `core/autocomplete/filtering/streamTransforms/lineStream.ts` L664

```typescript
export async function* noDoubleNewLine(lines: LineStream): LineStream {
  let isFirstLine = true;
  for await (const line of lines) {
    if (line.trim() === "" && !isFirstLine) {
      return; // ← 见到空行就终止整个 pipeline！
    }
    isFirstLine = false;
    yield line;
  }
}
```

Python 中语句间常有空行 (PEP 8 风格)。LLM 输出: `line1 + \n + \n + line2` → 经过该过滤器后只剩 `line1`（可能只有一个换行符或空白） → rawLen=1 → `PostReject reason=isBlank`。

这是日志中大量 `rawLen=0` / `rawLen=1` + `PostReject isBlank` 的 **主要来源**。

#### 2f. 推理后端 prefill 延迟 vs showWhatever 超时竞争

prompt 长度 ~8000 tokens 时，vLLM / sglang 这类推理后端的 prefill 都可能达到 200-500ms。`showWhateverWeHaveAtXMs()` 存在双重问题：(a) 从 pipeline **开始**计时（含 prefill 等待），(b) `StreamTransformPipeline.ts` L75 传入的值是 `modelTimeout` 而非专用展示超时。如果 prefill 期间没有 token 到达，实际留给 decode 的窗口极短或为零。

### 修复方案

**方案 A (P0): 修复 `noDoubleNewLine()`**

改为 **允许单个空行**，仅在连续双空行时停止：

```typescript
export async function* noDoubleNewLine(lines: LineStream): LineStream {
  let isFirstLine = true;
  let lastLineWasEmpty = false;

  for await (const line of lines) {
    const isEmpty = line.trim() === "";
    if (isEmpty && lastLineWasEmpty && !isFirstLine) {
      return; // 连续两个空行才停止
    }
    if (isEmpty && !isFirstLine) {
      lastLineWasEmpty = true;
      yield line;
      continue;
    }
    lastLineWasEmpty = false;
    isFirstLine = false;
    yield line;
  }
}
```

**方案 B (P1): PyCharm 增加 abort 信号传播**

在 `ContinueCompletionService.kt` 中传递取消信号给 core，使 `withTimeoutOrNull()` 超时后能通知 core 停止 LLM 请求。

**方案 C (P2): VS Code / PyCharm debouncer 统一收敛为协调层策略**

在引入请求协调层后，再统一改造两端 debouncer：

1. `pause` 分支不再硬编码 30ms，而是受协调层的动态下限控制
2. 若已有 in-flight 请求，或最近 `streamMs` / TTFT 偏高，则提升下限
3. 仅在“当前无在飞请求 + 最近完成足够快”时才允许快速响应

这样 debounce 就不再是单独堆出来的 heuristics，而是统一调度策略的一部分。

---

## 问题 3: 自动补全出半截（语句不完整）

### 根因 1 (主要): `showWhateverWeHaveAtXMs()` 超时截断 — 双重问题

**文件**: `core/autocomplete/filtering/streamTransforms/lineStream.ts` L643

```typescript
export async function* showWhateverWeHaveAtXMs(
  lines: LineStream,
  ms: number,
): LineStream {
  const startTime = Date.now(); // ← 计时从 pipeline 开始！
  let firstNonWhitespaceLineYielded = false;

  for await (const line of lines) {
    yield line;
    if (!firstNonWhitespaceLineYielded && line.trim() !== "") {
      firstNonWhitespaceLineYielded = true;
    }
    const isTakingTooLong = Date.now() - startTime > ms;
    if (isTakingTooLong && firstNonWhitespaceLineYielded) {
      break; // ← 超时截断
    }
  }
}
```

**问题 A**: `startTime` 在 pipeline 开始计时，包含了推理后端 prefill 等待时间。对于 8000 token prompt，在 vLLM / sglang 上 prefill 可能 200-400ms。如果 `modelTimeout=300ms`，实际留给 decode 生成的窗口只有 0-100ms。

**问题 B (更关键)**: `StreamTransformPipeline.ts` L75 传入的超时值是 `modelTimeout`，而非专用的展示超时配置：

```typescript
const timeoutValue = helper.options.modelTimeout; // L75 — 用了 modelTimeout!
// ...
showWhateverWeHaveAtXMs(lineGenerator, timeoutValue!); // L77
```

也就是说，不仅计时起点过早，使用的超时值本身也不合适——`modelTimeout` 通常比合理的 "展示已有内容" 超时更短。

**日志证据**: PyCharm 第一个请求生成 `if response.status_code != 20` (38 chars, 缺 `0:`)，streamMs=2440ms（使用了另一个模型 Qwen3-Coder-Next-AWQ-8bit）。

### 根因 2: `noDoubleNewLine()` 空行截断

LLM 输出两行代码中间有空行 → 第一行之后的空行触发 return → 只显示第一行。

### 根因 3: Stop token 意外命中

当前 stop tokens 包含 `/src/`、`#- coding: utf-8`、` ``` `。如果 Python 代码中出现文件路径包含 `/src/`，会意外截断。

### 修复方案

**方案 A (P0): 修改计时起点 + 使用独立超时配置**

1. 将 `startTime` 延迟到首 token 到达：

```typescript
export async function* showWhateverWeHaveAtXMs(
  lines: LineStream,
  ms: number,
): LineStream {
  let startTime: number | null = null; // ← 延迟到首 token
  let firstNonWhitespaceLineYielded = false;

  for await (const line of lines) {
    if (startTime === null) {
      startTime = Date.now(); // ← 从 LLM 首 token 到达开始计时
    }
    yield line;
    if (!firstNonWhitespaceLineYielded && line.trim() !== "") {
      firstNonWhitespaceLineYielded = true;
    }
    const isTakingTooLong = Date.now() - startTime > ms;
    if (isTakingTooLong && firstNonWhitespaceLineYielded) {
      break;
    }
  }
}
```

2. 在 `StreamTransformPipeline.ts` 中将超时值从 `modelTimeout` 改为独立配置：

```typescript
// 当前 (L75):
const timeoutValue = helper.options.modelTimeout;
// 改为:
const timeoutValue =
  helper.options.showWhateverTimeout ?? helper.options.modelTimeout;
```

**方案 B (P1): 审查 `/src/` stop token**

`/src/` 可能在 Python 文件路径中出现（如 `from src.utils import ...`），考虑移除或改为仅在行首匹配。

---

## 问题 4: 单行补全多、多行补全少

### 根本原因: `noDoubleNewLine()` 过滤器

Python 代码风格要求函数体内语句间有空行 (PEP 8)。LLM 生成的多行代码几乎必然包含空行：

```python
# LLM 生成:
x = compute_value()
                        # ← 空行 (Python 惯例)
y = process(x)         # ← 永远不会显示给用户
return y
```

`noDoubleNewLine()` 见到空行就 `return` → 只允许第一行通过。

### 次要原因: `shouldCompleteMultiline()` 分类器

**文件**: `core/autocomplete/classification/shouldCompleteMultiline.ts`

- 如果在单行注释 `//` 内 → 强制单行
- 如果 IntelliSense 补全项被选中 → 强制单行
- 语言特定规则可能限制多行

### 修复方案

同上问题 2 方案 A — 修改 `noDoubleNewLine()` 允许单个空行。这是解决单行/多行比例失调的 **核心修复**。

---

## 问题 5: Prompt 中没有 git diff 内容

### 根本原因: 代码中被故意禁用

**文件**: `core/autocomplete/snippets/getAllSnippets.ts` L235

```typescript
[], // racePromise(getDiffSnippets(ide)) // temporarily disabled, see https://github.com/continuedev/continue/pull/5882,
```

v1 (`getAllSnippets`) 和 v2 (`getAllSnippetsWithoutRace`) 版本均被禁用。

### 禁用原因 (PR #5882)

1. **大 diff 无用**: diff 越大，与当前光标位置补全的关联性越低
2. **大小不可预测**: 无法在获取前估计 diff 大小，可能挤占高价值上下文
3. **JetBrains 卡顿**: 生成/缓存大 diff 导致 IDE 卡住 (#5819)
4. **token 预算浪费**: diff 优先级低 (priority=98)，但可能消耗大量预算

即使启用，`filtering.ts` 也限制 diff 最多占 30% 预算：

```typescript
const maxKeyTokens =
  key === "diff" ? Math.floor(getRemainingTokenCount(helper) * 0.3) : Infinity;
```

### 影响

用户的 git diff 上下文完全丢失。对于理解"用户当前在改什么"的意图推断能力降低。日志中始终 `diff=0(0t)`。

### 修复方案

**方案 A (P2): 有限度恢复 diff snippets**

恢复 diff，但限制范围：

1. 只取当前文件的 diff（不取整个 repo 的 diff）
2. token 预算硬限 200 tokens
3. 超过 200ms 超时丢弃

```typescript
// getAllSnippets.ts
const diffSnippets = await racePromise(
  getDiffSnippetsForCurrentFile(ide, helper.filepath), // 仅当前文件
  200, // 200ms 超时
);
```

**方案 B (P2): 利用已有的 recentlyEditedRanges 替代**

`recentlyEditedRanges` 已经捕获了用户最近的编辑内容，与 diff 有 ~70% 重叠。可以增加 `editedRanges` 的 token 预算从 25% 到 30%，部分弥补 diff 缺失。

---

## 问题 6: Prompt 中缺少调用函数的签名/入参/返回值

### 根本原因: ImportDefinitionsService 在当前环境基本不工作

日志中始终 `import=0(0t)`。

### 最直接根因: cache miss 无同步兜底

autocomplete 取 import snippets 时优先读 cache；如果 cache miss，直接返回空结果。`ImportDefinitionsService` 在 `onDidChangeActiveTextEditor` 时异步填充 cache，但如果异步填充失败（gotoDefinition 超时、binary IPC 延迟等），cache 就始终为空。

### 底层失败链路（解释为什么 cache 始终为空）

```
ImportDefinitionsService._getFileInfo()
  → tree-sitter parse (需要 import-queries/python.scm)
  → 如缺失 → return { imports: {} }

  → query.matches() 提取 import 符号
  → ide.gotoDefinition() 解析每个符号
    → binary 模式走 IPC → 延迟高 → 150ms 超时后返回空

  → cache.get() 返回 undefined (异步预热未完成或已失败)

外层:
  → getAllSnippetsWithoutRace() 中
  → racePromise(contextRetrievalService.getSnippetsFromImportDefinitions(helper), 200)
  → 整个 import 解析超过 200ms → 全部丢弃返回空数组
```

### 设计局限

即使 ImportDefinitionsService 完全正常工作，它也**仅覆盖**：

- import 语句中声明的符号

**不覆盖** (当前完全缺失)：

- 当前函数体中调用的其他函数的签名
- 局部/内置函数的定义
- 返回值类型信息
- 参数说明和 docstring
- 类方法的 self 引用

### 修复方案

**方案 A (P1, 最高优先): cache miss 时同步兜底初始化**

在 `getSnippetsFromImportDefinitions()` 中，如果 cache 未就绪，同步执行一次 `_getFileInfo()` 而非返回空：

```typescript
public async getSnippetsFromImportDefinitions(helper: HelperVars) {
  let fileInfo = this.importDefinitionsService.get(helper.filepath);
  if (!fileInfo) {
    // Cache miss: 同步执行一次，但加超时保护
    fileInfo = await Promise.race([
      this.importDefinitionsService._getFileInfo(helper.filepath),
      new Promise(r => setTimeout(() => r(null), 300)),
    ]);
  }
  // ... 继续处理
}
```

**方案 B (P1): 增加 gotoDefinition 超时**

将 `ImportDefinitionsService.ts` 中的 150ms 超时改为 300ms（binary/IPC 模式下 150ms 太短）。应在 cache miss 兆底实现后再评估。

**方案 C (P2): 扩展到函数体内调用的函数**

目前只处理 import 语句声明的符号。可扩展为：

1. 从 prefix/suffix 中提取被调用的函数名（通过简单正则或 tree-sitter）
2. 对每个函数名尝试 gotoDefinition
3. 提取定义处的函数签名（def 行 + docstring 首行）作为上下文 snippet

---

## 问题 7: Qwen3 tokenizer 中可用 token 分析

### 完整 token 列表 (151643-151668)

来源: [Qwen3-Coder-30B-A3B-Instruct/tokenizer_config.json](https://www.modelscope.cn/models/Qwen/Qwen3-Coder-30B-A3B-Instruct/resolve/master/tokenizer_config.json)

| ID     | Token                    | Special | 当前状态         | 建议                         |
| ------ | ------------------------ | ------- | ---------------- | ---------------------------- |
| 151643 | `<\|endoftext\|>`        | Yes     | stop token ✓     | —                            |
| 151644 | `<\|im_start\|>`         | Yes     | stop token ✓     | —                            |
| 151645 | `<\|im_end\|>`           | Yes     | stop token ✓     | —                            |
| 151646 | `<\|object_ref_start\|>` | Yes     | 未使用           | 不需要 (多模态)              |
| 151647 | `<\|object_ref_end\|>`   | Yes     | 未使用           | 不需要 (多模态)              |
| 151648 | `<\|box_start\|>`        | Yes     | 未使用           | 不需要 (多模态)              |
| 151649 | `<\|box_end\|>`          | Yes     | 未使用           | 不需要 (多模态)              |
| 151650 | `<\|quad_start\|>`       | Yes     | 未使用           | 不需要 (多模态)              |
| 151651 | `<\|quad_end\|>`         | Yes     | 未使用           | 不需要 (多模态)              |
| 151652 | `<\|vision_start\|>`     | Yes     | 未使用           | 不需要 (多模态)              |
| 151653 | `<\|vision_end\|>`       | Yes     | 未使用           | 不需要 (多模态)              |
| 151654 | `<\|vision_pad\|>`       | Yes     | 未使用           | 不需要 (多模态)              |
| 151655 | `<\|image_pad\|>`        | Yes     | 未使用           | 不需要 (多模态)              |
| 151656 | `<\|video_pad\|>`        | Yes     | 未使用           | 不需要 (多模态)              |
| 151657 | **`<tool_call>`**        | No      | **未使用**       | **应加为 stop token**        |
| 151658 | `</tool_call>`           | No      | 未使用           | 可加为 stop token            |
| 151659 | `<\|fim_prefix\|>`       | No      | stop token ✓     | —                            |
| 151660 | `<\|fim_middle\|>`       | No      | stop token ✓     | —                            |
| 151661 | `<\|fim_suffix\|>`       | No      | stop token ✓     | —                            |
| 151662 | `<\|fim_pad\|>`          | No      | stop token ✓     | —                            |
| 151663 | `<\|repo_name\|>`        | No      | stop + prompt ✓  | —                            |
| 151664 | `<\|file_sep\|>`         | No      | stop + prompt ✓  | —                            |
| 151665 | `<tool_response>`        | No      | 未使用           | 可加为 stop token            |
| 151666 | `</tool_response>`       | No      | 未使用           | 可加为 stop token            |
| 151667 | **`<think>`**            | No      | **后处理已剥离** | **建议加为 stop token (P2)** |
| 151668 | **`</think>`**           | No      | **后处理已剥离** | **建议加为 stop token (P2)** |

### 关键发现

#### `<think>` / `</think>` — 建议加为 stop token (P2 增强项)

Qwen3 系列引入了思考链 (Chain-of-Thought) 功能。在 FIM 模式下，如果模型意外进入思考模式，会生成：

```
<think>
Let me analyze the code structure...
The function needs to handle error cases...
</think>
actual_code_here
```

**注意**: `postprocessing/index.ts` L151-155 **已有** `<think>` 剥离逻辑：

```typescript
if (llm.model.includes("qwen3")) {
  completion = completion.replace(/<think>.*?<\/think>/s, "");
  completion = completion.replace(/<\/think>/, "");
}
```

因此 `<think>` 内容不会展示给用户。加 stop token 的价值在于：**在生成阶段**提前终止，避免模型浪费 token 在思考链上（改善延迟），但不是当前 "补全为空/截断" 问题的根因。属于稳健性增强，非核心修复。

#### `<tool_call>` — 应阻止

防止模型在补全中生成工具调用语法，如：

```
<tool_call>
<function=search_code>
<parameter=query>error handling</parameter>
</function>
</tool_call>
```

### 修复方案

**方案 (P2): 在 `qwenCoderMultifileFimTemplate` 和 `qwenCoderFimTemplate` 添加 stop tokens**

作为稳健性增强项，在生成阶段即阻止思考链/工具调用 token（改善延迟），与后处理的 `<think>` 剥离形成双重保障。

**文件**: `core/autocomplete/templating/AutocompleteTemplate.ts`

```typescript
// qwenCoderMultifileFimTemplate.completionOptions.stop 添加:
stop: [
  // 现有 tokens...
  "<|endoftext|>",
  "<|fim_prefix|>",
  "<|fim_middle|>",
  "<|fim_suffix|>",
  "<|fim_pad|>",
  "<|repo_name|>",
  "<|file_sep|>",
  "<|im_start|>",
  "<|im_end|>",
  // 新增:
  "<think>",       // Qwen3 思考链 — 防止推理内容混入补全
  "</think>",      // 思考链结束
  "<tool_call>",   // 工具调用 — 防止生成工具调用语法
],
```

同理修改 `qwenCoderFimTemplate`。

---

## 修复优先级总表 (经 GPT 评审后修订)

### P0 — 必须立即修复（直接影响补全成功率和完整性）

| #   | 修复项                                                                    | 文件                         | 预期效果                          |
| --- | ------------------------------------------------------------------------- | ---------------------------- | --------------------------------- |
| 1   | 修改 `noDoubleNewLine()` 允许单个空行                                     | `lineStream.ts`              | 多行补全率预计从 ~10% 提升到 ~40% |
| 2   | `showWhateverWeHaveAtXMs()` 计时改为从首 token 到达开始                   | `lineStream.ts`              | 半截补全率预计下降 50%+           |
| 3   | StreamTransformPipeline 传入的超时值从 `modelTimeout` 改为 **独立配置项** | `StreamTransformPipeline.ts` | 给 decode 阶段更完整的时间窗口    |
| 4   | ~~abort error 日志降级为 debug~~ 先不做这个                               | `llm/index.ts`               | 消除 ~80% 日志噪声，便于后续调试  |

### P1 — 影响大，需要设计

| #   | 修复项                                     | 文件                                                                           | 预期效果                            |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------ | ----------------------------------- |
| 5   | 引入 latest-only 请求准入 / 生命周期协调层 | `CompletionProvider.ts` / `CompletionStreamer.ts` / `GeneratorReuseManager.ts` | 从架构上降低病态重叠请求与 abort 率 |
| 6   | JetBrains autocomplete 请求增加取消传播    | `ContinueCompletionService.kt`                                                 | 使 JetBrains 侧接入统一请求生命周期 |
| 7   | ImportDefs cache miss 时同步兜底初始化     | `ImportDefinitionsService.ts`                                                  | import 定义上下文从 0 变为 >0       |
| 8   | 评估 gotoDefinition 超时 150ms→300ms       | `ImportDefinitionsService.ts`                                                  | 提高 cache 填充成功率               |
| 9   | 审查 `/src/` stop token 是否存在误伤       | `getStopTokens.ts`                                                             | 减少 Python 路径中的意外截断        |

### P2 — 优化项

| #   | 修复项                                               | 文件                                        | 预期效果                      |
| --- | ---------------------------------------------------- | ------------------------------------------- | ----------------------------- |
| 10  | VS Code / JetBrains debouncer 统一收敛为协调层策略   | `AutocompleteDebouncer.ts` / `Debouncer.kt` | 减少快速打字时的无效请求      |
| 11  | JetBrains URI 规范化 (`mock:///` → `file://`)        | JetBrains 扩展层 / core template 层         | PyCharm 虚拟文件上下文恢复    |
| 12  | 有限恢复 diff snippets（仅当前文件）                 | `getAllSnippets.ts`                         | 恢复编辑意图上下文            |
| 13  | 为 Qwen 添加 `<think>` / `<tool_call>` stop token    | `AutocompleteTemplate.ts`                   | 减少无效 token 生成，改善延迟 |
| 14  | 推理后端 cancel/abort 能力抽象（兼容 vLLM / sglang） | `llm/index.ts` + `core/llm/llms/*.ts`       | 支持服务端提前释放或优雅回退  |

## 变更文件清单 (所有问题)

| 文件                                                                      | 涉及问题 | 变更内容                                                        |
| ------------------------------------------------------------------------- | -------- | --------------------------------------------------------------- |
| `core/autocomplete/filtering/streamTransforms/lineStream.ts`              | #2,#3,#4 | 修改 `noDoubleNewLine()` + `showWhateverWeHaveAtXMs()` 计时     |
| `core/autocomplete/filtering/streamTransforms/StreamTransformPipeline.ts` | #3       | 超时值从 `modelTimeout` 改为独立配置                            |
| `core/llm/index.ts` / `core/llm/llms/*.ts`                                | #1       | abort 日志从 error 降级为 debug + 推理后端取消能力抽象          |
| `core/autocomplete/CompletionProvider.ts`                                 | #1,#2    | 引入请求准入 / 生命周期协调逻辑                                 |
| `core/autocomplete/generation/CompletionStreamer.ts`                      | #1,#2    | 将流式阶段接入统一请求生命周期                                  |
| `core/autocomplete/generation/GeneratorReuseManager.ts`                   | #1,#2    | 将 generator reuse 降为协调层下的一个策略分支                   |
| `core/autocomplete/util/AutocompleteDebouncer.ts`                         | #1,#2    | 从独立 heuristics 改为协调层输入策略                            |
| `core/autocomplete/context/ImportDefinitionsService.ts`                   | #6       | cache miss 同步兜底 + 超时容错                                  |
| `core/autocomplete/templating/getStopTokens.ts`                           | #3       | 审查 `/src/` stop token                                         |
| `extensions/intellij/.../ContinueCompletionService.kt`                    | #2       | 添加 abort signal 传播                                          |
| `extensions/intellij/.../Debouncer.kt`                                    | #2       | 复用统一的请求协调策略输入                                      |
| JetBrains 扩展层 / core template 层                                       | #2       | `mock:///` URI 规范化                                           |
| `core/autocomplete/snippets/getAllSnippets.ts`                            | #5       | 有条件恢复 diff snippets                                        |
| `core/autocomplete/templating/AutocompleteTemplate.ts`                    | #7       | 添加 `<think>`, `</think>`, `<tool_call>` stop tokens (P2 增强) |

## 验证清单

- [ ] 修改 `noDoubleNewLine()` 后，验证 Python 代码补全可产出多行（在同一测试场景复现）
- [ ] 修改 `showWhateverWeHaveAtXMs()` 计时 + 独立超时后，对比补全完整性（半截率应下降）
- [ ] abort 日志降级后，确认 error 日志噪声减少 ~80%
- [ ] 引入请求协调层后，确认同一文档 / 模型不会同时存在多个病态 in-flight 生成
- [ ] 区分 `abortBeforeFirstToken` 与 `abortAfterFirstToken`，确认剩余 abort 中哪些只是健康的 typing churn，哪些是病态重叠请求
- [ ] 在统一协调层下，再验证 VS Code 的 `debounceWaitMs` 不再长期钉死在 29-32ms
- [ ] 在 PyCharm 同一场景重新测试，对比成功率（目标: 从 36% 提升到 60%+）
- [ ] 观察推理后端日志（vLLM / sglang），确认 abort 数量下降；若后端支持 server-side cancel，再确认资源回收提前发生
- [ ] 确认 `import=0(0t)` 变为 `import>0` 在 cache miss 兜底实现后
- [ ] 添加 stop tokens 后，用 Qwen3-Coder 触发补全，确认对延迟有改善

---

## 实施记录 (2026-04-09)

### 已完成：P0 三项修复

#### P0-1: `noDoubleNewLine()` — 允许单个空行

**文件**: `core/autocomplete/filtering/streamTransforms/lineStream.ts`

**修改前**: 见到任何空行 (非首行) 即 `return` 终止整个流。  
**修改后**: 跟踪 `lastLineWasEmpty` 状态，仅在**连续两个空行**时 `return`。单个空行正常 `yield`。

**根因评估**: ✅ 根因修复。Python 单空行是 PEP 8 规范要求的代码风格，原实现把正常代码风格当成终止条件。新实现区分"单空行"(yield) 与"连续双空行"(终止)，直接消除根因。

**预期效果**: 多行补全率从 ~10% 提升到 ~40%+。`rawLen=0/1` + `PostReject isBlank` 的占比应大幅下降。

#### P0-2: `showWhateverWeHaveAtXMs()` — 计时从首 token 到达开始

**文件**: `core/autocomplete/filtering/streamTransforms/lineStream.ts`

**修改前**: `const startTime = Date.now()` 在函数入口 (pipeline 创建时)，prefill 200-500ms 侵蚀 decode 窗口。  
**修改后**: `let startTime: number | null = null`，延迟到 `for await` 收到首条 line 时才赋值 `Date.now()`。

**根因评估**: ✅ 根因修复。问题本质是"计时起点包含了不应计入的推理后端 prefill 时间"。新实现只计从首 token 到达后的 decode 时间，给生成阶段完整的时间窗口。

**预期效果**: 半截补全率下降 50%+。8K token prompt 时不再出现"prefill 吃完超时，decode 0ms 窗口"的极端情况。

#### P0-3: `StreamTransformPipeline` — 使用正确的超时配置项

**文件**: `core/autocomplete/filtering/streamTransforms/StreamTransformPipeline.ts`

**修改前**: `helper.options.modelTimeout` (Line 74) — 用推理超时值当展示超时。  
**修改后**: `helper.options.showWhateverWeHaveAtXMs ?? helper.options.modelTimeout` — 优先使用已定义的展示超时配置。

**根因评估**: ✅ 根因修复。`TabAutocompleteOptions.showWhateverWeHaveAtXMs` 配置项和默认值 (`parameters.ts` 中 300ms general, 500ms ZKDEV) 早已存在，只是引用错了字段。一行配置引用修正，不是补丁。

**预期效果**: `modelTimeout=300ms` 环境下实际展示超时从 300ms (含 prefill) 提升到 500ms (仅 decode 时间)。与 P0-2 叠加后，给 decode 阶段更充分的窗口。

### 变更文件

| 文件                         | 行                          | 变更                                       |
| ---------------------------- | --------------------------- | ------------------------------------------ |
| `lineStream.ts`              | `noDoubleNewLine()`         | 单空行 yield，双空行终止                   |
| `lineStream.ts`              | `showWhateverWeHaveAtXMs()` | startTime 延迟到首 token                   |
| `StreamTransformPipeline.ts` | L74                         | `modelTimeout` → `showWhateverWeHaveAtXMs` |

### 测试验证要点

- [ ] P0-1: Python 补全中出现单个空行时不被截断（函数体内多行补全）
- [ ] P0-2: 8K prompt 场景下补全不再出半截
- [ ] P0-3: 确认 `showWhateverWeHaveAtXMs` 配置值 (500ms) 被正确使用
- [ ] 回归: 非 Python 语言（JS/TS）补全不受影响
- [ ] 回归: 连续双空行仍能正确终止补全流（不产生垃圾输出）
